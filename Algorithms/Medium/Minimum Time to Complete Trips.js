/**
 * @param {number[]} time
 * @param {number} totalTrips
 * @return {number}
 */
var minimumTime = function(time, totalTrips) {
    let count = 0;

    for (let i = 0; i < totalTrips; i++) {

    }

    return count;
}

const c = minimumTime([1,2,3], 5)

console.log(c)